# test123 `dev` 分支代码问题清单

- **检查日期**：2026-09-08
- **检查分支**：`dev`
- **检查提交**：`09d32ed Add sorting algorithm examples`
- **检查范围**：`algorithms/` 新增排序程序、测试和说明，以及与现有 `blog/` 工程的集成关系

## 总体结论

三种排序算法在当前测试范围内能够正确排序数字和自定义比较器，且不会修改输入数组。现有 Node.js 语法检查、示例运行和 11 项自动化测试均通过，没有发现明显的安全问题或会导致基础用例立即失败的严重逻辑错误。

当前主要问题集中在**运行环境边界、输入契约、测试覆盖和工程化配置**，而不是基础排序流程本身。

## 问题清单

### ISSUE-001：排序模块没有接入网页工程（中）

**位置**：`algorithms/sorting.js`、`blog/index.html`、`blog/script.js`

**现象**：排序代码使用 CommonJS 的 `module.exports` 和 `require.main`，只能直接作为 Node.js 程序或 CommonJS 模块使用；现有博客页面没有引用或调用这些算法，也没有排序演示页面。

**影响**：如果需求是让网页用户在浏览器中使用或查看排序效果，直接通过 `<script>` 引入该文件会因 `module`/`require` 不存在而失败。目前实现更接近独立的 Node.js 算法示例，而不是博客工程中的网页功能。

**建议**：明确运行目标。如果需要浏览器演示，改为 ES module，或增加一个独立的 `blog/algorithm-demo.html` 与页面脚本；如果只需要 Node.js 示例，应在根目录文档中明确说明它与博客页面是两个独立入口。

---

### ISSUE-002：默认比较器对非法数据静默失效（中）

**位置**：`algorithms/sorting.js` 中的 `numberCompare`

**现象**：默认比较器实现为 `a - b`，但没有校验元素是否为有效数字。对于字符串或 `NaN` 等输入，比较结果可能是 `NaN`，算法会把它当作“不需要交换”，而不是报错。

例如：

```js
bubbleSort([3, Number.NaN, 2]);
// [3, NaN, 2]，仍然不是有序结果
```

**影响**：调用者可能误以为程序完成了排序，却得到未排序的数据，问题不容易定位。

**建议**：二选一：

1. 在默认数字模式下检查每个元素，遇到非有限数字时抛出带上下文的 `TypeError`；
2. 保持通用实现，但把比较器设为必传，避免对数据类型做隐含假设。

同时在测试中补充 `NaN`、字符串和混合类型输入的预期行为。

---

### ISSUE-003：缺少项目级运行脚本和 Node 版本约束（中）

**位置**：仓库根目录

**现象**：项目没有 `package.json`、根级 README 或统一的测试命令。测试依赖 Node 内置 `node:test`，但没有声明最低 Node.js 版本，也没有 CI 配置来自动执行测试。

**影响**：新成员需要记住具体路径和命令；不同 Node 版本可能无法运行测试；代码推送后不会自动发现回归问题。

**建议**：增加根目录 `package.json`，至少提供：

```json
{
  "scripts": {
    "test": "node --test algorithms/sorting.test.js"
  },
  "engines": {
    "node": ">=18"
  }
}
```

再根据仓库使用方式增加 GitHub Actions，在提交或 Pull Request 时执行测试。

---

### ISSUE-004：测试覆盖了基础行为，但没有验证关键算法特性（低）

**位置**：`algorithms/sorting.test.js`

**现象**：现有测试覆盖了数字排序、空数组、单元素、自定义比较器、异常输入和不修改原数组，但没有明确覆盖：

- 已排序数组和逆序数组；
- 全部元素相同的数组；
- 大规模输入及极端分区；
- 冒泡排序和插入排序声称的稳定性；
- 快速排序在重复值和对象比较器下的行为；
- 比较器返回非法值时的行为。

**影响**：后续修改分区逻辑、比较器处理或优化时，现有测试可能无法及时捕获边界回归。

**建议**：增加带稳定 ID 的对象数组来验证稳定性；加入确定性的边界数据和大数组测试；必要时加入固定随机种子的属性测试，并断言每种算法的输出与基准排序结果一致。

---

### ISSUE-005：快速排序热路径存在不必要的自交换（低）

**位置**：`algorithms/sorting.js` 中的 `swap` 和 `quickSort` 分区循环

**现象**：`swap` 使用数组解构交换，每次调用都会创建临时解构结构；快速排序在 `smaller === current` 时仍会调用 `swap`，因此会对自身进行无实际效果的交换。大数组或需要大量交换的输入会放大这部分额外开销。

**影响**：不影响排序结果，但会增加快速排序热路径的操作和临时对象开销，降低教学实现的性能表现。

**建议**：使用局部临时变量交换元素，并在两个索引相同的时候跳过交换：

```js
if (first !== second) {
  const temporary = items[first];
  items[first] = items[second];
  items[second] = temporary;
}
```

同时保留针对大数组的基准测试，确认优化没有改变分区逻辑。

---

### ISSUE-006：算法实现和博客工程的文档入口分散（低）

**位置**：`algorithms/README.md`、`blog/README.md`

**现象**：排序程序的说明只放在 `algorithms/README.md`，博客说明只放在 `blog/README.md`，仓库根目录没有说明两个子项目之间的关系、入口和测试方式。

**影响**：访问仓库首页的开发者不容易知道排序程序如何运行，也不容易判断它是否属于博客页面功能。

**建议**：增加根级 README，列出目录结构、博客预览命令、排序示例命令、测试命令和 Node 版本要求。

## 优先级建议

1. **优先处理 ISSUE-002**：明确非法输入行为，避免返回看似成功但实际未排序的结果。
2. **随后处理 ISSUE-003**：补充统一脚本、版本约束和 CI，建立可重复验证流程。
3. 根据产品目标决定是否处理 **ISSUE-001**：如果排序算法只是学习示例，可以保留独立 Node.js 入口；如果要成为网页功能，则需要浏览器演示和页面集成。
4. 用 **ISSUE-004/005/006** 完善性能、测试和维护文档。

## 已执行验证

```text
node --check algorithms/sorting.js       通过
node algorithms/sorting.js               通过
node --test algorithms/sorting.test.js   11 项全部通过
随机正确性检查                         300 次通过
```
